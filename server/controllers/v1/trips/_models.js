

/**
 * @swagger
 * definition:
 *
 *   trip:
 *     properties:
 *       _id:
 *         type: string
 *       distance_travelled:
 *         type: integer
 *       driver_rating:
 *         type: integer
 *       rider_rating:
 *         type: integer
 *       start_zip_code:
 *         type: string
 *       end_zip_code:
 *         type: string
 *       charity_id:
 *         type: string
 *       requested_car_category:
 *         type: string
 *       free_credit_used:
 *         type: integer
 *       surge_factor:
 *         type: integer
 *       color:
 *         type: string
 *       make:
 *         type: string
 *       model:
 *         type: string
 *       year:
 *         type: integer
 *       rating:
 *         type: integer
 *       Date:
 *         type: string
 *       PRCP:
 *         type: number
 *         format: double
 *       TMAX:
 *         type: number
 *         format: double
 *       TMIN:
 *         type: number
 *         format: double
 *       AWND:
 *         type: number
 *         format: double
 *       GustSpeed2:
 *         type: number
 *         format: double
 *       Fog:
 *         type: number
 *         format: double
 *       HeavyFog:
 *         type: number
 *         format: double
 *       Thunder:
 *         type: number
 *         format: double
 *       start:
 *         type: object
 *         properties:
 *           type:
 *             type: string
 *           coordinates:
 *             type: array
 *             items:
 *               type: number
 *               format: double
 *       end:
 *         type: object
 *         properties:
 *           type:
 *             type: string
 *           coordinates:
 *             type: array
 *             items:
 *               type: number
 *               format: double
 *       complete_date:
 *         type: string
 *       start_date:
 *         type: string
 *
 */
